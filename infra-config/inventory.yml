# file: inventory.yml
proxmox:
  hosts:
    pve0[1:3]:

dhcp_server:
  hosts:
    matchbox:

unbound_servers:
  hosts:
    unbound02:

backup_servers:
  hosts:
    backup:

freeipa:
  hosts:
    infra01:

ipaserver:
  hosts:
    infra01.int.mk-labs.cloud:

# matchbox:
#   hosts:
#     infra01:

hub_cluster:
  hosts:
    ocp-hub:

sql_servers:
  hosts:
    sql01:

n8n_servers:
  hosts:
    n8n:

docker_servers:
  hosts:
    docker01:

observer:
  hosts:
    prometheus:

target:
  hosts:
    prometheus:
