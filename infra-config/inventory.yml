# file: inventory.yml
proxmox:
  hosts:
    pve0[1:3]:

unbound_servers:
  hosts:
    unbound02:

backup_servers:
  hosts:
    backup:

freeipa:
  hosts:
    infra01:

matchbox:
  hosts:
    infra01:


hub_cluster:
  hosts:
    ocp-hub:

sql_servers:
  hosts:
    sql01:

n8n_servers:
  hosts:
    n8n:

docker_servers:
  hosts:
    docker01: