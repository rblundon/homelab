apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
    - names:
      - api.hub.int.mk-labs.cloud
      servingCertificate:
        name: letsencrypt
